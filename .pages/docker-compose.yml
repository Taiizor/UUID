services:
  app:
    build:
      context: .
      target: build-stage
    ports:
      - "4398:4398"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: npm run serve